# mihrab is a personal open-source project: always resolve from the public npm
# registry, regardless of any machine-global registry override (e.g. a private
# CodeArtifact default). Contains no secrets.
registry=https://registry.npmjs.org/
